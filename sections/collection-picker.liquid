{% for product in collections['mycollect'].products %}
    {{ product.featured_image | image_url: width:200 | image_tag }}
    {{ product.price| money }}
    {{ product.compare_at_price | money}}
{% endfor %}