{% for product in collections['mycollect'].products %}
    <p>{{ product.featured_image | image_url: width:200 | image_tag }}</p>
    <p>{{ product.price| money }}</p>
    <p>{{ product.compare_at_price | money}}</p>
{% endfor %}